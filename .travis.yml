language: python
install:
  - pip install -r requirements.txt
  - pip install coveralls
script:
  - python manage.py collectstatic --no-input
  - coverage run manage.py test accounts chat
after_success:
  coveralls
