language: python

language: python
services:
– postgresql
python:
– 2.7
install:
– pip install -r requirements.txt
before_script:
– psql -c 'create database pite_project;' -U postgres
– python manage.py makemigrations
script:
– python manage.py test